version: '3.8'
services:
  wine-api:
    volumes:
      - .:/app  # Монтируем код для hot-reload
    environment:
      - ENV=development
    command: uvicorn api.app:app --host 0.0.0.0 --port 5000 --reload